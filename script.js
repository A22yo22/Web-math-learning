"use strict"

let inputs = document.getElementById("inputs");

let firstMultiplyer = 1;
let secondMultiplyer = 1;

let whatsToCalculate = document.createTextNode(`Was ist ${firstMultiplyer}  mal ${secondMultiplyer}?`);

inputs.appendChild(whatsToCalculate);